# User-invoked：lazy-teach 不自動觸發

`lazy-teach` 設為 `disable-model-invocation: true`，只有使用者手動呼叫才啟動。替代方案是 model-invoked（agent 偵測「除錯卡關」自動開課），被否決：除錯到一半被強制上課會打斷心流，「此刻該不該學」是使用者的判斷。代價是零 context 負擔換使用者認知負擔——要記得它存在。此決定與 /teach 家族慣例一致。
